type SignUpResponse {
    ok: Boolean!
    error: String
}

type Mutation {
    SignUp(
        userId: String!,
        password: String!,
        familyName: String!,
        givenName: String!,
    ): SignUpResponse!
}
