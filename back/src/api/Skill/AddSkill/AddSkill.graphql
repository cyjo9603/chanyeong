type AddSkillResponse {
    ok: Boolean!
    error: String
    skill: Skill
}

type Mutation {
    AddSkill(
        name: String!,
        type: SkillType!,
        level: Int!,
        description: String!
        icon: String!
    ): AddSkillResponse!
}