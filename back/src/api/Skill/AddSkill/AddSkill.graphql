type AddSkillResponse {
  ok: Boolean!
  error: String
}

type Mutation {
  AddSkill(
    name: String!
    type: SkillType!
    level: Int!
    description: String!
    icon: String!
    order: Int!
  ): AddSkillResponse! @auth
}
