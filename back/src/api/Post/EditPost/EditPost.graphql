type EditPostResponse {
    ok: Boolean!
    error: String
}

type Mutation {
    EditPost(
        id: Int!
        category: PostCategory
        title: String
        content: String
        titleImage: String
        deleteTags: [Int]
        addTags: [Int]
    ): EditPostResponse!
}